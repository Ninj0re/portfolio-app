version: '3.8'

services:
  portfolio:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: portfolio-app
    ports:
      - "3000:80"
    restart: unless-stopped
    networks:
      - portfolio-network

networks:
  portfolio-network:
    driver: bridge
